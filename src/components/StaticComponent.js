import React from "react";

const StaticComponent = () =>{
    return  (
        <div id="staticMessage">
            <p>Here we have a Static message, could be useful to print some info
                <br/> that is not necessary to includes on a Component</p>
        </div>
    );
} 

export default StaticComponent;